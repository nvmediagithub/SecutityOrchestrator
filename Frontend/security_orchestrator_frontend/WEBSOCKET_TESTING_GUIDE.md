# WebSocket Testing Guide для SecurityOrchestrator Flutter

## Исправленные проблемы

### 1. Несоответствие протоколов
- **Проблема**: Flutter использовал обычный WebSocket, backend - STOMP
- **Решение**: Добавлен STOMP клиент в Flutter (`stomp_dart` библиотека)

### 2. Неправильный endpoint
- **Проблема**: URL не соответствовал backend конфигурации
- **Решение**: Используем STOMP endpoint `/ws/executions` с правильной конфигурацией

### 3. Обработка ошибок
- **Проблема**: Нет retry логики и graceful degradation
- **Решение**: Добавлена экспоненциальная retry логика с максимальными попытками

## Файлы изменений

### Новые файлы
- `lib/core/network/stomp_client.dart` - STOMP совместимый WebSocket клиент

### Обновленные файлы
- `pubspec.yaml` - добавлена зависимость `stomp_dart: ^0.4`
- `lib/presentation/providers/process_provider.dart` - замена WebSocket на STOMP
- `lib/presentation/screens/execution_monitoring_screen.dart` - обновленные вызовы STOMP

## Тестирование

### 1. Проверка backend
```bash
cd SecutityOrchestrator/Backend
./gradlew bootRun
```
Ожидаемый вывод:
```
Started SecurityOrchestratorApplication in 5.123 seconds
WebSocket configured: /ws/executions
```

### 2. Установка зависимостей Flutter
```bash
cd SecutityOrchestrator/Frontend/security_orchestrator_frontend
flutter pub get
```

### 3. Запуск Flutter приложения
```bash
flutter run
```

## Диагностика проблем

### Проверка WebSocket подключения
1. Откройте мониторинг выполнения в приложении
2. Проверьте статус подключения в правом верхнем углу:
   - 🟢 **Live** - STOMP подключен
   - 🔴 **Offline** - STOMP отключен

### Логи для отладки
STOMP клиент выводит подробные логи:
```
STOMP connected successfully
Subscribed to execution: [executionId]
STOMP disconnected
STOMP error: Connection refused
```

### Типичные проблемы и решения

#### Проблема: "WebSocketException: Failed to connect WebSocket"
**Причина**: Backend не запущен
**Решение**: 
1. Убедитесь что backend запущен на порту 8080
2. Проверьте что WebSocket endpoint доступен

#### Проблема: "STOMP server is not running"
**Причина**: STOMP сервис не инициализирован
**Решение**:
1. Проверьте что `WebSocketConfig` правильно настроен
2. Убедитесь что `ExecutionWebSocketController` доступен

#### Проблема: Сообщения не приходят
**Причина**: Неправильная подписка
**Решение**:
1. Проверьте STOMP destination `/topic/executions/{id}`
2. Убедитесь что backend отправляет сообщения на правильные topic'и

## Мониторинг подключения

### Автоматическое переподключение
STOMP клиент автоматически:
- Переподключается при разрыве связи
- Использует экспоненциальную задержку (5с, 10с, 15с...)
- Останавливается после 10 неудачных попыток

### Graceful degradation
При недоступности STOMP:
- Приложение продолжает работать
- Показывается статус "Offline"
- Пользователь может повторно подключиться

### Методы для проверки состояния
```dart
final stompClient = ref.read(stompClientProvider);
print('Connected: ${stompClient.isConnected}');
print('Status: ${stompClient.getConnectionStatus()}');
print('Failures: ${stompClient.failureCount}');
```

## Тестирование различных сценариев

### 1. Успешное подключение
- Запустите backend
- Запустите Flutter
- Перейдите к мониторингу выполнения
- Ожидайте статус "Live"

### 2. Разрыв соединения
- Остановите backend во время работы Flutter
- Проверьте статус "Offline"
- Перезапустите backend
- Ожидайте автоматическое переподключение

### 3. Недоступность сети
- Отключите интернет
- Проверьте статус "Offline"
- Включите интернет
- Ожидайте переподключение

## Дополнительные возможности

### Отладка STOMP сообщений
Включите debug логи в `stomp_client.dart`:
```dart
onDebugMessage: (message) {
  print('STOMP Debug: $message');
},
```

### Ручное управление подключением
```dart
// Подключение
stompClient.connect();

// Отключение
stompClient.disconnect();

// Проверка сети
final isOnline = await stompClient.checkNetworkConnectivity();
```

### Обработка пользовательских ошибок
```dart
stompClient.onError = (error) {
  if (error.toString().contains('Connection refused')) {
    // Показать диалог с инструкциями
    _showConnectionErrorDialog();
  }
};